import { Router } from "express";import { authenticate,requireRole } from "../auth/auth.middleware.js";import { UserRole } from "../users/user.types.js";import { validate } from "../../middleware/validate.middleware.js";import * as c from "./information.controller.js";import * as v from "./information.validator.js";
export const informationRouter=Router();
informationRouter.get("/explore",c.explore);informationRouter.get("/notices",validate(v.publicNoticeQuerySchema),c.notices);informationRouter.get("/notices/:id",validate(v.idSchema),c.notice);informationRouter.get("/guides",validate(v.publicGuideQuerySchema),c.guides);informationRouter.get("/guides/:id",validate(v.idSchema),c.guide);
export const informationAdminRouter=Router();informationAdminRouter.use(authenticate,requireRole(UserRole.ADMINISTRATOR));informationAdminRouter.get("/notices",validate(v.adminNoticeQuerySchema),c.adminNotices);informationAdminRouter.post("/notices",validate(v.createNoticeSchema),c.createNotice);informationAdminRouter.patch("/notices/:id",validate(v.updateNoticeSchema),c.updateNotice);informationAdminRouter.get("/guides",validate(v.adminGuideQuerySchema),c.adminGuides);informationAdminRouter.post("/guides",validate(v.createGuideSchema),c.createGuide);informationAdminRouter.patch("/guides/:id",validate(v.updateGuideSchema),c.updateGuide);
