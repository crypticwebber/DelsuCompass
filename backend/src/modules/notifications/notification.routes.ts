import { Router } from "express";import { authenticate,requireRole } from "../auth/auth.middleware.js";import { UserRole } from "../users/user.types.js";import * as c from "./notification.controller.js";export const notificationRouter=Router();notificationRouter.use(authenticate,requireRole(UserRole.STUDENT));notificationRouter.get("/",c.list);notificationRouter.get("/unread-count",c.count);notificationRouter.patch("/read-all",c.readAll);notificationRouter.patch("/:id/read",c.readOne);
