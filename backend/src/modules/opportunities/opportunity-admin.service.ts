import { Types } from "mongoose";import { AppError } from "../../utils/AppError.js";import { OpportunityModel } from "./opportunity.model.js";import { OpportunityReportModel } from "./opportunity-report.model.js";function valid(id:string){if(!Types.ObjectId.isValid(id))throw new AppError(404,"NOT_FOUND","Item not found")}
export const opportunityAdminService={async create(adminId:string,input:Record<string,unknown>){return OpportunityModel.create({...input,submittedBy:adminId,status:"approved",moderatedBy:adminId,moderatedAt:new Date()})},async posts(status?:string){return OpportunityModel.find(status&&status!=="undefined"?{status}:{}).populate("submittedBy","fullName email faculty department level").sort({createdAt:-1}).limit(200).lean()},async moderate(adminId:string,id:string,status:string,reason?:string){valid(id);const p=await OpportunityModel.findById(id);if(!p)throw new AppError(404,"OPPORTUNITY_NOT_FOUND","Opportunity not found");p.status=status as any;p.moderatedBy=new Types.ObjectId(adminId) as any;p.moderatedAt=new Date();p.rejectionReason=status==="rejected"?reason:undefined;await p.save();return p},async reports(status?:string){return OpportunityReportModel.find(status&&status!=="undefined"?{status}:{}).populate("opportunityId","title type status deadline").populate("reportedBy","fullName email").sort({createdAt:-1}).limit(200).lean()},async resolveReport(adminId:string,id:string,status:string,note?:string){valid(id);const r=await OpportunityReportModel.findById(id);if(!r)throw new AppError(404,"REPORT_NOT_FOUND","Opportunity report not found");r.status=status as any;r.resolutionNote=note;r.resolvedBy=new Types.ObjectId(adminId) as any;r.resolvedAt=new Date();await r.save();return r},async stats(){const [pending,approved,rejected,openReports]=await Promise.all([OpportunityModel.countDocuments({status:"pending"}),OpportunityModel.countDocuments({status:"approved"}),OpportunityModel.countDocuments({status:"rejected"}),OpportunityReportModel.countDocuments({status:"pending"})]);return{pending,approved,rejected,openReports}}};
