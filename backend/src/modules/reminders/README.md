# Reminders module

One configurable class reminder can be attached to each student-owned timetable entry.
The persisted value is `minutesBefore`, allowing the client/notification layer to calculate recurring weekly reminder occurrences without duplicating class schedule data.
