import { NavLink } from "react-router-dom";
import { mobileStudentNav } from "./nav.config";
export function MobileBottomNav(){return <nav className="fixed inset-x-0 bottom-0 z-30 border-t border-blue-100 bg-white/92 px-2 pb-[max(0.55rem,env(safe-area-inset-bottom))] pt-2 backdrop-blur-xl lg:hidden" aria-label="Mobile navigation"><div className="mx-auto grid max-w-lg grid-cols-5 gap-1">{mobileStudentNav.map(({label,to,icon:Icon})=><NavLink key={to} to={to} className={({isActive})=>`flex min-w-0 flex-col items-center gap-1 rounded-2xl px-1 py-2 text-[10px] font-bold ${isActive?"bg-blue-50 text-blue-800":"text-slate-400 hover:bg-slate-50 hover:text-slate-700"}`}><Icon className="h-5 w-5" aria-hidden="true"/><span className="truncate">{label}</span></NavLink>)}</div></nav>}
