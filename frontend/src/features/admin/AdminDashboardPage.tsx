import { useQuery } from "@tanstack/react-query";
import { AlertTriangle, BellRing, CalendarDays, CheckCircle2, Clock3, House, MapPinned, MessageSquareText, ShieldAlert, Sparkles, UsersRound } from "lucide-react";
import { Link } from "react-router-dom";
import { useAuth } from "@/features/auth/AuthProvider";
import { adminApi } from "./admin.api";

function Card({label,value,sub}:{label:string;value:number;sub:string}){return <div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[.13em] text-slate-400">{label}</p><strong className="mt-2 block text-3xl font-semibold tracking-tight text-slate-950">{value}</strong><p className="mt-1 text-xs text-slate-500">{sub}</p></div>}
export function AdminDashboardPage(){const{user}=useAuth();const overview=useQuery({queryKey:["admin","overview"],queryFn:adminApi.overview,refetchInterval:120_000});const d=overview.data;const queues=[{to:"/admin/accommodation",label:"Accommodation",value:(d?.moderation.pendingAccommodation??0)+(d?.moderation.pendingAccommodationReviews??0)+(d?.moderation.accommodationReports??0),icon:House},{to:"/admin/community",label:"Community",value:(d?.moderation.pendingCommunity??0)+(d?.moderation.communityReports??0),icon:MessageSquareText},{to:"/admin/events",label:"Events",value:(d?.moderation.pendingEvents??0)+(d?.moderation.eventReports??0),icon:CalendarDays},{to:"/admin/opportunities",label:"Opportunities",value:(d?.moderation.pendingOpportunities??0)+(d?.moderation.opportunityReports??0),icon:Sparkles},{to:"/admin/safety",label:"Safety",value:d?.moderation.pendingSafetyReports??0,icon:ShieldAlert}];const totalQueue=queues.reduce((n,x)=>n+x.value,0);return <main className="p-5 md:p-8"><div className="mx-auto max-w-7xl"><header className="rounded-[2rem] border border-blue-100 bg-gradient-to-br from-blue-50 to-white p-6 sm:p-8"><p className="text-xs font-semibold uppercase tracking-[.2em] text-blue-700">Administrator workspace</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.03em] text-slate-950 md:text-4xl">Good day, {user?.fullName?.split(" ")[0]||"Administrator"}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-600">Review student submissions, manage public campus information and keep moderation queues moving from one clear workspace.</p></header>{overview.isError&&<div className="mt-6 rounded-2xl border border-red-100 bg-red-50 p-4 text-sm font-semibold text-red-700">The administrator overview could not be loaded.</div>}<section className="mt-7 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Card label="Registered students" value={d?.users.totalStudents??0} sub={`${d?.users.activeStudents??0} active accounts`}/><Card label="Moderation queue" value={totalQueue} sub="Items requiring attention"/><Card label="Critical safety" value={d?.moderation.criticalSafetyReports??0} sub={`${d?.publicContent.activeSafetyAlerts??0} active public alerts`}/><Card label="Verified map points" value={d?.publicContent.activeLocations??0} sub="Active DELSU/Abraka locations"/></section><section className="mt-7 grid gap-5 xl:grid-cols-[1.35fr_.65fr]"><div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Moderation</p><h2 className="mt-1 text-xl font-semibold">Work queues</h2></div><Clock3 className="h-5 w-5 text-slate-400"/></div><div className="mt-4 grid gap-3 sm:grid-cols-2">{queues.map(({to,label,value,icon:Icon})=><Link key={to} to={to} className="flex items-center justify-between rounded-2xl border border-slate-200 bg-slate-50/60 p-4 transition hover:border-blue-200 hover:bg-blue-50"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-blue-100 text-blue-700"><Icon className="h-5 w-5"/></span><div><p className="font-semibold text-slate-900">{label}</p><p className="text-xs text-slate-400">Open workspace</p></div></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${value?"bg-amber-100 text-amber-800":"bg-blue-100 text-blue-700"}`}>{value}</span></Link>)}</div></div><div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Account health</p><h2 className="mt-1 text-xl font-semibold">Students</h2><div className="mt-5 space-y-3"><Status icon={CheckCircle2} label="Active" value={d?.users.activeStudents??0}/><Status icon={BellRing} label="Unverified email" value={d?.users.unverifiedStudents??0}/><Status icon={AlertTriangle} label="Disabled" value={d?.users.disabledStudents??0}/></div><Link to="/admin/users" className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-blue-700 px-4 py-3 text-sm font-semibold text-white"><UsersRound className="h-4 w-4"/>Manage students</Link></div></section><section className="mt-5 grid gap-5 xl:grid-cols-2"><div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Public information</p><h2 className="mt-1 text-xl font-semibold">Published content</h2><div className="mt-4 grid grid-cols-2 gap-3 text-sm">{[["Accommodation",d?.publicContent.approvedAccommodation??0],["Community posts",d?.publicContent.approvedCommunity??0],["Events",d?.publicContent.approvedEvents??0],["Opportunities",d?.publicContent.approvedOpportunities??0],["Safety alerts",d?.publicContent.activeSafetyAlerts??0],["Map locations",d?.publicContent.activeLocations??0]].map(([label,value])=><div key={String(label)} className="rounded-2xl bg-blue-50 p-4"><strong className="text-xl font-semibold text-blue-900">{value}</strong><p className="mt-1 text-xs text-slate-500">{label}</p></div>)}</div></div><div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">Recent registrations</p><h2 className="mt-1 text-xl font-semibold">Newest students</h2></div><UsersRound className="h-5 w-5 text-slate-400"/></div><div className="mt-4 space-y-3">{d?.recentStudents?.length?d.recentStudents.map(s=><div key={s.id} className="flex items-center justify-between rounded-2xl border border-slate-100 bg-slate-50 p-3"><div className="min-w-0"><p className="truncate text-sm font-semibold">{s.fullName}</p><p className="truncate text-xs text-slate-400">{s.department||"Department not set"}{s.level?` · ${s.level}L`:""}</p></div><span className={`ml-3 rounded-full px-2 py-1 text-[10px] font-semibold uppercase ${s.isVerified?"bg-blue-100 text-blue-700":"bg-amber-100 text-amber-800"}`}>{s.isVerified?"verified":"unverified"}</span></div>):<p className="text-sm text-slate-500">No student registrations yet.</p>}</div></div></section></div></main>}
function Status({icon:Icon,label,value}:{icon:any;label:string;value:number}){return <div className="flex items-center justify-between rounded-2xl bg-slate-50 p-3"><div className="flex items-center gap-2 text-sm font-medium text-slate-600"><Icon className="h-4 w-4 text-slate-400"/>{label}</div><strong className="font-semibold">{value}</strong></div>}
