import { useState } from "react";import { useQuery } from "@tanstack/react-query";import { Search,MapPin,CalendarDays,House,ShieldAlert,Users,BriefcaseBusiness,ArrowRight,BookOpenText } from "lucide-react";import { Link } from "react-router-dom";import { searchApi } from "./search.api";
const filters=["all","information","accommodation","community","events","opportunities","safety","locations"] as const;const labels:Record<string,string>={all:"All",information:"Official info",accommodation:"Accommodation",community:"Community",events:"Events",opportunities:"Opportunities",safety:"Safety",locations:"Locations"};const icons:any={information:BookOpenText,accommodation:House,community:Users,events:CalendarDays,opportunities:BriefcaseBusiness,safety:ShieldAlert,locations:MapPin};
export function SearchPage(){const [q,setQ]=useState("");const [type,setType]=useState("all");const trimmed=q.trim();const query=useQuery({queryKey:["global-search",trimmed,type],queryFn:()=>searchApi.search(trimmed,type),enabled:trimmed.length>=2,staleTime:30000});return <section className="mx-auto max-w-5xl space-y-6"><div><p className="text-sm font-bold uppercase tracking-[.18em] text-blue-700">Compass Search</p><h1 className="mt-2 text-3xl font-black tracking-tight">Find what you need around DELSU</h1><p className="mt-2 text-slate-600">Search official DELSU information, accommodation, community posts, events, opportunities, safety alerts and campus locations from one place.</p></div><div className="rounded-[2rem] border border-slate-200 bg-white p-5 shadow-sm"><div className="relative"><Search className="absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-slate-400"/><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Try ‘library’, ‘internship’, ‘Site III’..." className="w-full rounded-2xl border border-slate-200 bg-slate-50 py-4 pl-12 pr-4 text-base outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-50"/></div><div className="mt-4 flex flex-wrap gap-2">{filters.map(f=><button key={f} onClick={()=>setType(f)} className={`rounded-full px-4 py-2 text-sm font-semibold ${type===f?"bg-slate-900 text-white":"bg-slate-100 text-slate-600 hover:bg-slate-200"}`}>{labels[f]}</button>)}</div></div>{trimmed.length<2?<div className="rounded-[2rem] border border-dashed border-slate-300 bg-white p-12 text-center"><Search className="mx-auto h-7 w-7 text-slate-400"/><p className="mt-3 font-bold">Start typing to search DELSU Compass</p><p className="mt-1 text-sm text-slate-500">Enter at least two characters.</p></div>:query.isLoading?<div className="rounded-2xl bg-white p-8 text-center text-slate-500">Searching across Compass…</div>:query.isError?<div className="rounded-2xl border border-red-200 bg-red-50 p-5 text-red-700">Search could not be completed. Please try again.</div>:query.data?.total===0?<div className="rounded-[2rem] border border-dashed border-slate-300 bg-white p-12 text-center"><p className="font-bold">No results for “{trimmed}”</p><p className="mt-1 text-sm text-slate-500">Try another keyword or search all categories.</p></div>:<div className="space-y-6"><p className="text-sm font-semibold text-slate-500">{query.data?.total} result{query.data?.total===1?"":"s"} found</p>{query.data?.groups.filter(g=>g.items.length).map(g=>{const Icon=icons[g.type]||Search;return <div key={g.type} className="rounded-[2rem] border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-50 text-blue-700"><Icon className="h-4 w-4"/></span><h2 className="text-lg font-black">{labels[g.type]||g.type}</h2><span className="text-sm text-slate-400">{g.items.length}</span></div><div className="divide-y divide-slate-100">{g.items.map(item=><Link key={item.id} to={item.link} className="group flex gap-4 py-4 first:pt-0 last:pb-0"><div className="min-w-0 flex-1"><h3 className="font-bold text-slate-900 group-hover:text-blue-700">{item.title}</h3><p className="mt-1 text-xs font-semibold uppercase tracking-wide text-blue-700">{item.subtitle}</p><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-600">{item.description}</p></div><ArrowRight className="mt-1 h-5 w-5 shrink-0 text-slate-300 group-hover:text-blue-600"/></Link>)}</div></div>})}</div>}</section>}
